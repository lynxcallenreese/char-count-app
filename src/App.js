import React from 'react';
import CharacterCounter from './CharacterCounter'; // Assuming CharacterCounter component is in a separate file

function App() {
  return (
    <div className="App">
    <h1 style={{textAlign: 'center'}}>Character Counter App</h1>
      <CharacterCounter />
    </div>
  );
}

export default App;
